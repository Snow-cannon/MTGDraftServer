exports.load_cube = function(cube_data) {

    if(true){
        //Return ok: true if the cube is valid. Return data if necessary
        return { ok: true }
    } else {
        //Return ok: false if the cube is not valid (Too few cards)
        return { ok: false }
    }
}